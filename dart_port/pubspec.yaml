name: ai2go_dart
version: 0.1.0
publish_to: 'none'
description: Dart/Flutter-friendly port of ai2go core AI loop and tool-calling flow.

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  http: ^1.2.2

dev_dependencies:
  lints: ^4.0.0
